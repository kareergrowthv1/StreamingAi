# Leave empty when using Vite proxy (dev on same host)
# VITE_API_BASE=
# VITE_WS_BASE=

# When calling backend directly (e.g. different port)
# VITE_API_BASE=http://127.0.0.1:9000
# VITE_WS_BASE=ws://127.0.0.1:9000
